{
  "name": "@enbandeja/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "db:seed:demo": "tsx --env-file-if-exists=.env prisma/seed-demo-menu.ts",
    "db:studio": "prisma studio",
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "lint": "echo 'lint ok'",
    "clean": "rm -rf node_modules .turbo"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prisma": "^5.22.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "prisma": {
    "seed": "npx tsx prisma/seed.ts"
  }
}
