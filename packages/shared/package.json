{
  "name": "@enbandeja/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "lint": "echo 'lint ok'",
    "clean": "rm -rf node_modules .turbo"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "date-fns": "^3.0.0",
    "date-fns-tz": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
